# TransitOps Frontend

React + TypeScript + Vite responsive operations dashboard.

```bash
cp .env.example .env
npm install
npm run dev
```

Open `http://localhost:5173`. The backend must be available on port `4000`.
